PORT=3000

# ⚠️ OBLIGATOIRE EN PRODUCTION - Générer avec:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGEZ_MOI_EN_PRODUCTION_64_CARACTERES_ALEATOIRES_MINIMUM

# MongoDB Atlas (recommandé) - NE JAMAIS COMMITER LA VRAIE URI!
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/jeu_bleu_rouge?retryWrites=true&w=majority

# URL de l'application (adapter selon l'environnement)
APP_URL=https://jeu-bleu-rouge.onrender.com

# Configuration Email (optionnel - pour vérification email)
EMAIL_SERVICE=gmail
EMAIL_USER=votre_email@gmail.com
# Utiliser un "App Password" Gmail, pas le mot de passe principal
EMAIL_PASSWORD=mot_de_passe_application_gmail

# Alternative SMTP personnalisé
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

